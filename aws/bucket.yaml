---
AWSTemplateFormatVersion: '2010-09-09'
Description: 'S3 bucket for the website static content'

Parameters:
  ProjectName:
    Type: String
    Description: 'The name of the project, used for naming resources'
  DomainName:
    Type: String
    Description: 'The domain name and the S3 bucket name at the same time'

Outputs:
  BucketArn:
    Description: 'The ARN of the S3 bucket'
    Value: !GetAtt WebsiteBucket.Arn
  BucketDomainName:
    Description: 'The website endpoint domain name of the S3 bucket, used as the CloudFront origin for automatic index-document resolution'
    Value: !Select [2, !Split ['/', !GetAtt WebsiteBucket.WebsiteURL]]

Resources:
  WebsiteBucket:
    UpdateReplacePolicy: 'Delete'
    Type: 'AWS::S3::Bucket'
    DeletionPolicy: 'Delete'
    Properties:
      AbacStatus: 'Disabled'
      WebsiteConfiguration:
        IndexDocument: 'index.html'
      PublicAccessBlockConfiguration:
        RestrictPublicBuckets: false
        IgnorePublicAcls: false
        BlockPublicPolicy: false
        BlockPublicAcls: false
      BucketName: !Ref DomainName
      OwnershipControls:
        Rules:
          - ObjectOwnership: 'BucketOwnerEnforced'
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - BucketKeyEnabled: true
            ServerSideEncryptionByDefault:
              SSEAlgorithm: 'AES256'
      Tags:
        - Key: 'Project'
          Value: !Ref ProjectName
        - Key: 'Name'
          Value: !Ref DomainName

  WebsiteBucketPolicy:
    UpdateReplacePolicy: 'Delete'
    Type: 'AWS::S3::BucketPolicy'
    DeletionPolicy: 'Delete'
    Properties:
      Bucket: !Ref WebsiteBucket
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Resource: !Sub 'arn:aws:s3:::${DomainName}/*'
            Action: 's3:GetObject'
            Effect: 'Allow'
            Principal: '*'
            Sid: 'PublicReadGetObject'